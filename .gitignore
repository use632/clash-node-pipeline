# Generated output (subscriptions, reports, logs, and your saved subscription URLs)
/output/

# Compiled binaries
*.exe
*.exe~
# root-level binary only (leading slash so it does NOT match the cmd/clash-node-pipeline/ package dir)
/clash-node-pipeline

# Personal/live config files (keep them locally, don't publish)
configs/live-*.yaml

# Go workspace / test artifacts
*.test
*.out
/vendor/

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
